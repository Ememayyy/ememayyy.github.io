# Codeforces Daily Momentum

GitHub Pages-ready build.

The production `index.html` embeds the CSS and JavaScript so the site does not depend on GitHub Pages resolving local asset paths. `style.css` and `app.js` are also included as source files.

Deploy: GitHub Pages -> Deploy from branch -> `main` -> `/ (root)`.
